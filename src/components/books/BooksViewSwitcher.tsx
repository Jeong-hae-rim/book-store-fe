import { styled } from "styled-components";

export default function BooksViewSwitcher() {
  return (
    <>
      <BooksViewSwitcherStyle>BooksViewSwitcher</BooksViewSwitcherStyle>
    </>
  );
}

const BooksViewSwitcherStyle = styled.div``;
